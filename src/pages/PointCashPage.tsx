import { PageHeader } from '@/components/common/bits'

/**
 * СТАБ. Каркас розділу «Каса точки» (`21 §Н17`, `M45` з боку точки): наділ грошима,
 * скільки в касі, скільки не хватає до наділу, зміна й перерахунок. Команди стора
 * (`setCashFloat`, `openShift`, `countCash`, `closeShift`) уже написані — тут поки
 * лише заголовок, щоб маршрут і навігація існували до того, як екран намалюють.
 *
 * Заголовок мусить ДОСЛІВНО дорівнювати підпису розділу в навігації — smoke чекає
 * саме власний заголовок розділу, а не будь-який <h1>.
 *
 * Жодного числа і жодного виклику форматера: порожня каса й каса, якої ще не рахували,
 * — різні стани, і «0,00» стер би цю різницю.
 */
export function PointCashPage() {
  return (
    <div className="mx-auto max-w-[1000px]">
      <PageHeader title="Каса точки" />
      <p className="text-sm text-muted-foreground">Розділ у роботі</p>
    </div>
  )
}
