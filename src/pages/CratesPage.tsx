import { PageHeader } from '@/components/common/bits'

/**
 * СТАБ. Каркас розділу «Ящики на точці» (`21 §Н14`, `M40`): наділ, пусті на точці,
 * у людей, у нас із ягодою, видача й повернення. Рушій (`calc.ts`) і команди стора
 * (`issueCrates`, `returnCrates`, `postShipment`) уже написані — тут поки лише
 * заголовок, щоб маршрут і навігація існували до того, як екран намалюють.
 *
 * Заголовок мусить ДОСЛІВНО дорівнювати підпису розділу в навігації — smoke чекає
 * саме власний заголовок розділу, а не будь-який <h1>.
 *
 * Жодного числа і жодного виклику форматера: показувати «0» там, де даних ще немає,
 * означало б збрехати про залишок.
 */
export function CratesPage() {
  return (
    <div className="mx-auto max-w-[1000px]">
      <PageHeader title="Ящики" />
      <p className="text-sm text-muted-foreground">Розділ у роботі</p>
    </div>
  )
}
