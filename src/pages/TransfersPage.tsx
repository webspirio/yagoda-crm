import { PageHeader } from '@/components/common/bits'

/**
 * СТАБ. Каркас розділу «Перекази» (`21 §Н18`, `M44` і `M45` з боку керівника):
 * заборгованість перед точками, надсилання грошей і ящиків, «Прийняв» на точці.
 * Команди стора (`sendTransfer`, `acceptTransfer`, `disputeTransfer`, `voidTransfer`)
 * уже написані — тут поки лише заголовок, щоб маршрут і навігація існували до того,
 * як екран намалюють.
 *
 * Заголовок мусить ДОСЛІВНО дорівнювати підпису розділу в навігації — smoke чекає
 * саме власний заголовок розділу, а не будь-який <h1>.
 *
 * Розділ лише керівницький і приходить окремим чанком (`React.lazy` в `App.tsx`),
 * тому приймальник на точці його байтів не тягне.
 */
export function TransfersPage() {
  return (
    <div className="mx-auto max-w-[1000px]">
      <PageHeader title="Перекази" />
      <p className="text-sm text-muted-foreground">Розділ у роботі</p>
    </div>
  )
}
