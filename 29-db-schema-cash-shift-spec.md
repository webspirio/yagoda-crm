# 29 · Спека: каса, зміна і перекази в схемі БД

**Дата рішення:** 03.09.2026. **Обсяг:** три суперечності чинної схеми + частина 7 правил
(каса точки, зміна, перекази). **Джерело полів:** `src/lib/types.ts` — форми, які вже
пройшли реалізацію, 585 юніт-тестів і smoke; розходження з правилами названі поштучно.

**Результат станом на рішення 03.09.2026 (ранок):** 17 → 22 таблиці, 3 → **8 enum-ів**. Одна
таблиця замінюється (`shift_grade_prices` → `grade_prices`), шість додаються, три наявні
змінюються.

**Того ж дня власник переглянув частину цього дизайну** (розділ 10) і скасував три з шести
доданих таблиць — `cash_floats`, `crate_allotments`, `expected_cash_returns`. Enum-ів це не
торкнулося. **Фактичний результат: 17 → 19 таблиць**, 8 enum-ів, 43 зовнішні ключі, 179
полів — числами саме цей, а не 22, треба читати весь документ нижче.

Правила — `26-rules-by-example.md` (посилання «§N»), правки й запитання —
`26-правки-і-запитання.md` («правка N», «запитання N»). Схема — `28-db-schema.dbml`,
стиль діаграми — `28-db-schema.md`, призначення таблиць — `28-tables.md`.

---

## 1. Три суперечності, а не пропуски

Пропуски вже перелічені в `28-db-schema.md` («Що ця схема ще не покриває»). Нижче — те, що
чинна схема стверджує **проти** правила, тому це виправляється першим і незалежно від обсягу.

### 1.1 Ціна дня не може висіти на зміні

`shift_grade_prices.shift_id NOT NULL`, але за наскрізним днем (§1) керівник виставляє ціни
о **07:10**, а приймальник відкриває зміну о **07:30**: зміни ще не існує, коли ціна вже є.
Друге: `UNIQUE (shift_id, product_grade_id)` **забороняє історію**, тоді як §4.2 вимагає
протилежного дослівно — «кожна зміна лягає окремим записом із часом і автором; записи не
перетираються — додаються». Автора, часу й причини («конкуренти підняли») в таблиці немає
взагалі, як і `created_at`.

### 1.2 `target_cash` / `target_crates` числом із `default 0`

§6.1: «цільове значення — це історія, а не число», зі зміною 600 → 800 як **новою подією** і
причиною. §6.9 і §7.10 вимагають, щоб «не задано» було видно: у полі стоїть «—», а не нуль, і
точка без цільового значення каси **в таблицю боргу мережі не потрапляє взагалі**. `default 0`
робить «не задано» і «нуль» нерозрізненними, тобто прямо ламає обидва правила.

Ця суперечність **виправлена**, але не так, як пропонує розділ 4.2 нижче: після перегляду
власника 03.09.2026 (розділ 10) числа лишилися на `collection_points` — `target_cash` і
`target_crates` **nullable, без `default`**, без окремої таблиці й без `effective_from`.

### 1.3 §7.8 не має механізму, а §4.8 і §8.1 не мають поля

`shifts` не має жодного індексу, тому «дві зміни на одній точці заборонено» тримається лише
кодом. І `collection_points` не має роду пункту, хоча §4.8 робить склад пунктом прийому зі
своєю ціною, якого не чіпає «встановити всім», а §8.1 розрізняє пункт, **звідки** приїхала
ягода, і базу, **де** переважували.

---

## 2. Рішення і відкинуті альтернативи

Причини записані тому, що кожну з них легко «полагодити» назад у бік звичнішого рішення.

| рішення | відкинуто | чому |
| --- | --- | --- |
| **Ціна — журнал `grade_prices` на трійці (день, точка, сорт)** | лишити `shift_grade_prices`, зняти `UNIQUE`, додати автора | вимагало б, щоб зміна існувала до першої ціни, тобто або керівник відкриває зміну (§10.3 забороняє), або ціни ставляться після 07:30. Це зміна правила, а не схеми |
| | журнал + похідна таблиця «чинна ціна» | кеш — рівно те місце, де в книзі клієнта рвався ланцюжок (124 розриви з 1 473) |
| **Дві каси через enum `cash_book`** | пари колонок `berry_*` / `crate_*` | кожна нова згортка мусить згадати обидві колонки, і забути одну нічого не заважає; з enum суму без `GROUP BY cash_book` непомітно не напишеш |
| | дві окремі таблиці кас | дублює механіку перерахунку двічі — те саме, від чого пішли, лишивши `payout_allocations` і `crate_return_allocations` однієї форми навмисно |
| **Цільові значення — окремі таблиці `cash_floats`/`crate_allotments` з `effective_from`** *(обрано вранці 03.09.2026, скасовано того ж дня — розділ 10)* | одна `point_targets` з `kind` | **Крок 1, обрано:** одиниці різні (`numeric(12,2)` проти `int`); спільна колонка суми брехала б типом для ящиків. **Крок 2, скасовано:** власник — «наділ це лише показник, на який клієнт орієнтується всередині дня; в історії важливі фінансові операції, видачі ящиків тощо, а наділ — орієнтир»; цільове значення не факт про минуле, історія йому не потрібна. Фінально — два nullable-числа `target_cash`/`target_crates` на `collection_points`, без `effective_from` (розділ 4.2, розділ 10) |
| **Три моменти перерахунку — в одному `cash_counts`** | суми відкриття й закриття полями `shifts` | дало б 8 колонок (2 моменти × 2 книги × набране/очікуване) і **другий механізм** для жесту, який §7.6 уже вимагає окремим документом. У `cash_counts` «рівно один перерахунок на відкритті кожної книги» стає частковим унікальним індексом, а не домовленістю |
| **Таблиці рухів каси немає** | `cash_movements` | правка 9 дослівно: «система рахує загальну суму в касі за допомогою денних транзакцій». Збережений залишок заборонений тією ж причиною, що й `remaining` у `intakes` (§3.2) |
| **`discrepancy` не зберігається** | поле в `cash_counts` і `shifts` | маючи `counted_amount` і знімок `expected_amount`, розбіжність — арифметика. Рядок 12 конвенцій самого `.dbml`: «похідні суми НЕ зберігаються» |

---

## 3. Зміни у трьох наявних таблицях

```dbml
Enum point_kind { reception  base }
Enum shift_status { open  awaiting_explanation  closed }

Table collection_points {
  kind          point_kind [not null, default: 'reception']
  target_cash   numeric(12,2)   // nullable, без default — розділ 4.2
  target_crates int             // nullable, без default — розділ 4.2
}

Table shifts {
  status      shift_status [not null, default: 'open']
  explanation text
  indexes {
    (collection_point_id) [unique]        // partial: WHERE closed_at IS NULL — §7.8
    (collection_point_id, business_date)
  }
}

Table payouts {
  return_settled_at         timestamp
  return_settled_by_user_id uuid [ref: > users.id]
  return_note                text
}
```

`shifts.explanation` обовʼязкове при закритті з розбіжністю (§7.7 + правка 2: зміна
закривається, але з поясненням, і розбіжність у документі лишається — її не підганяють).
`closed_by_user_id` уже є, і при розбіжності це **керівник**, а не той, хто рахував.

`point_kind` має `default 'reception'`, бо пунктів прийому більшість, а база одна.

**`payouts` змінилася двічі того самого 03.09.2026.** Спершу — жодним полем: очікуване
повернення готівки мало жити окремою таблицею `expected_cash_returns` (первісний розділ 4.5).
Після перегляду власника (розділ 10) — трійкою полів прямо на `payouts`, яка вже стоїть у
`28-db-schema.dbml`. §9.3: «сторновано виплату 8 000,00 ₴ → каса **НЕ** виросла на 8 000 →
створюється очікуване повернення під фізичне внесення грошей. Інакше сторно стає способом
красти». Доки `return_settled_at` порожній, каса точки лишається зменшеною на суму виплати;
заповнення поля і є фізичним внесенням грошей назад. Сума **не дублюється** окремим полем —
вона завжди дорівнює `payouts.amount`, тому окрема таблиця з власним `amount` була б копією
того, що вже є. Форма трійки навмисно та сама, що в `void_*`: це другий клас полів, які
дописують **після** проведення документа, а не новий документ.

---

## 4. Три нові таблиці (і одна ідея, яка нею не стала)

Задумано було шість: `grade_prices`, `cash_floats`, `crate_allotments`, `cash_counts`,
`transfers`, `expected_cash_returns`. Після перегляду власника 03.09.2026 (розділ 10) три
таблиці скасовано — `cash_floats`, `crate_allotments` (4.2) та `expected_cash_returns`, чиї
поля переїхали трійкою на наявну `payouts` (розділ 3, третя змінена таблиця, не нова — тому й
не тут). Підрозділ 4.2 лишений на місці й переписаний: більше не описує нову таблицю, а
показує, чим замінилося те, що в ній планувалося. Справді новими лишаються три: 4.1
`grade_prices`, 4.3 `cash_counts`, 4.4 `transfers`.

### 4.1 `grade_prices` — ціна дня як журнал

```dbml
Table grade_prices {
  id uuid [pk]

  business_date       date [not null]
  collection_point_id uuid [not null, ref: > collection_points.id]
  product_grade_id    uuid [not null, ref: > product_grades.id]

  base_price numeric(10,2) [not null]

  set_at         timestamp [not null]
  set_by_user_id uuid [not null, ref: > users.id]
  reason         text

  created_at timestamp [not null]

  indexes {
    (collection_point_id, business_date, product_grade_id, set_at)
  }
}
```

`UNIQUE` тут немає **навмисно**: кілька записів на трійку — це і є вимога §4.2. Чинною
вважається остання за `set_at`. `updated_at` немає: таблиця append-only, рядок після вставки
не редагується ніколи.

**Два поля старої таблиці зникають, і це рішення.**

- `operator_price_delta` — §2.8 каже, що надбавку набирають **на рядку**
  (`intake_items.bonus`), а ціна дня в прийомці «під замком: приймальник її лише бачить».
  Тримати дельту ще й тут означало б ту саму надбавку двічі — дослівно той аргумент, яким уже
  відкинуто сорт «ОПТ» у Note `product_grades`.
- `is_enabled` — §4.5: «сорт без ціни дня на прийомці взагалі не показується», тобто
  **відсутність рядка** і є вимкненням. Довговічне вимкнення тримає `product_grades.is_active`;
  подобового вимкнення не вимагає жодне правило.

Прийомка бере ціну **знімком** у `intake_items.price`, тому журнал ніде не читається заднім
числом — саме тому §4.6 («раніші квитанції не перераховуємо») тримається сам, без коду.

**Ціна рішення, названа вголос:** `intakes` більше не має спільної зміни з ціною, тому
узгодженість «ціна тієї точки того дня» тримає застосунок, а не зовнішній ключ.

### 4.2 `target_cash` / `target_crates` — цільові значення числами, не історією

**Цього підрозділу в первісному вигляді більше немає — рішення скасовано.** Уранці
03.09.2026 тут стояли дві дзеркальні таблиці, `cash_floats` і `crate_allotments`, з
`effective_from`, `reason` і `UNIQUE (collection_point_id, effective_from)`; форма дзеркальна
була навмисно, бо клієнтка сказала «технологія з грошима така сама, як з ящиками». Того ж дня
власник це переглянув (розділ 10) — обидві таблиці прибрано з `28-db-schema.dbml`, а на їхньому
місці, назад на `collection_points` (розділ 3):

```dbml
target_cash   numeric(12,2)
target_crates int
```

`NOT NULL` і `default` тут навмисно відсутні — це та сама суперечність 1.2, виправлена, але
іншим способом, ніж пропонувала ця спека: §6.9 і §7.10 вимагають, щоб «не задано» лишалося
видимим (у полі «—», а не нуль), а `default: 0` зробив би «не задано» й «нуль»
нерозрізненними. Значення міняє лише керівник (§10.2); зміна, як і раніше, **не перераховує**
наявні звірки: звірка за 14 липня після зміни 15 липня лишається такою, якою була. Цільове
значення, менше за те, що вже в людей і в дорозі, дозволене з попередженням, а не забороною
(§6.1). Невстановлене цільове значення **не блокує** видачу ящиків, а лише показує
попередження — правка 14 (`26-правки-і-запитання.md`, рядок 1267), яка скасовує пряму заборону
з §9.1 «кнопка видачі неактивна»: забороняти видачу через порожній `target_crates` означало б
відтворити ту саму заборону, яку правка вже скасувала.

**Ціна цього рішення, названа вголос, — розділ 10**: без `effective_from` зміна цільового
значення діє відразу на всі дні, включно з минулими.

### 4.3 `cash_counts` — перерахунок як свідчення

```dbml
Enum cash_book { berry  crates }
Enum cash_count_kind { opening  midday  closing }

Table cash_counts {
  id uuid [pk]

  shift_id uuid [not null, ref: > shifts.id]

  book cash_book       [not null]
  kind cash_count_kind [not null]

  counted_amount  numeric(12,2) [not null]
  expected_amount numeric(12,2) [not null]

  counted_by_user_id uuid [not null, ref: > users.id]
  counted_at timestamp [not null]

  created_at timestamp [not null]

  indexes {
    (shift_id, book, kind) [unique]   // partial: WHERE kind <> 'midday'
    (shift_id, counted_at)
  }
}
```

Точки тут немає — вона у `shifts`, і два примірники одного факту в цьому проєкті заборонені
за побудовою. `expected_amount` — **знімок** очікуваного на мить перерахунку: без нього
пізніша подія тихо переписала б учорашню розбіжність. Розбіжність = `counted − expected`, і
поля вводу для неї немає в жодної ролі (§7.7); порогів «допустимої» розбіжності немає взагалі
— не зійшлося на копійку те саме, що на 350 ₴.

`counted_by_user_id` — той, **хто натиснув**, а не той, хто відкривав зміну (§10.6: якщо
касу перерахує Марія, у документі стоїть Марія). Перерахунок нічого не виправляє і нічого не
перетирає; робити його можна скільки завгодно разів — звідси `midday` поза унікальним
індексом. **Поля `note` немає** — жодне правило його не читає.

### 4.4 `transfers` — переказ база → точка

```dbml
Enum transfer_status { sent  accepted  disputed  void }

Table transfers {
  id uuid [pk]

  collection_point_id uuid [not null, ref: > collection_points.id]

  cash   numeric(12,2) [not null]
  crates int [not null]

  carrier varchar [not null]

  sent_by_user_id uuid [not null, ref: > users.id]
  sent_at timestamp [not null]

  status transfer_status [not null, default: 'sent']

  accepted_by_user_id uuid [ref: > users.id]
  accepted_date date
  accepted_at   timestamp

  reported_cash   numeric(12,2)
  reported_crates int
  dispute_note    text

  resolved_cash   numeric(12,2)
  resolved_crates int
  resolved_by_user_id uuid [ref: > users.id]
  resolved_at timestamp

  correction_of_transfer_id uuid [ref: > transfers.id]

  voided_at timestamp
  voided_by_user_id uuid [ref: > users.id]
  void_reason text

  created_at timestamp [not null]
  updated_at timestamp [not null]

  indexes { (collection_point_id, accepted_date)  (status) }
}
```

- **Поля `code` немає** *(рішення власника 03.09.2026)*: рейс ідентифікують **датою й
  перевізником**, а не номером — §7.9 описує переказ повністю і номера не згадує жодного разу,
  а паперового двійника немає, перевізник підписує в зошиті.
- `carrier` — **текст**, не обліковий запис: «Іван, Ducato» підписує в зошиті перевізник, а не
  користувач системи. Без перевізника документ не проводиться (§7.9).
- `accepted_date` — **день прийняття**, і саме ним зараховуються гроші та ящики: машина
  виїхала ввечері, точка порахувала вранці, і гроші не мають лежати в касі за день, коли їх
  фізично не було.
- Натиснути «Прийняв» може **тільки точка** (§10.3): керівник не може зробити це за неї.
- `reported_*` і `dispute_note` — те, що точка нарахувала при «Не сходиться». Це **інформація
  для керівника**, у формули не входить ніколи.
- `resolved_*` — відповідь на **запитання 4** і несуче поле: §7.9 крок 4б каже, що після
  підтвердження «у формулу обчислень потрапляє число, яке вказує керівник, закриваючи спір».
  Без цієї пари єдиним місцем для того числа стало б редагування `cash`, тобто тихе
  переписування проведеного документа.
- `correction_of_transfer_id` — виправлення це **новий документ**, а не правка старого.
- Сторнує переказ **тільки керівник**; точка сторнувати не може (§9.4).

---

## 5. Як рахується каса

Формули записані тут, бо в них лежить різниця, яку легко «виправити» у бік симетрії й тим
зламати §9.3.

**Книга ягоди** на точці, від `cash_book_from`:

```
+ Σ transfers, у яких accepted_date ≤ D:
      status = 'accepted'                          → cash
      status = 'disputed' І resolved_at IS NOT NULL → resolved_cash
− Σ payouts.amount, де payouts → shifts дає цю точку і shifts.business_date ≤ D
      — ВКЛЮЧНО ЗІ СТОРНОВАНИМИ
+ Σ payouts.amount, у яких return_settled_at ≤ D
```

**Сторновані виплати з формули каси НЕ відсіюються, і це протилежно до формули боргу.** Борг
(Note `payout_allocations`) вимагає відсіяти `voided_at IS NOT NULL`, бо гроші «не видали». Каса
вимагає протилежного: гроші **фізично вийшли з шухляди** і повертаються до неї лише внесенням,
тобто рядком `payouts.return_settled_at` (розділ 3, скасовано `expected_cash_returns.settled_at`
— розділ 10). Один і той самий `voided_at` у двох запитах читається по-різному, і саме тому
обидві формули стоять тут поруч.

**Книга ящиків** на точці, від першої видачі (без нижньої межі по датах, §7.5):

```
+ Σ crate_issuances.deposit_taken   (voided_at IS NULL)
− Σ crate_returns.deposit_refund    (voided_at IS NULL)
                          — обидві таблиці дають точку через shifts, як і виплати
```

**Відкрите, і не полагоджене цією спекою:** для ящиків сторно трактується протилежно до
виплати — відсторнована видача перестає давати гроші в касу, хоч завдаток фізично в шухляді, і
жодного «очікуваного повернення» під це немає. Правила такого випадку не описують: §9.3 знає
очікуване повернення лише для виплати, а §9.4 дозволяє сторнувати ящиковий документ **тільки
керівнику** й забороняє сторно видачі, на яку вже лягло повернення. Тобто асиметрія свідома й
названа, а не забута.

**Що НЕ рухає касу:** витрати дня (§7.2 — «каса тільки ягодами, ті всі різні розтрати я передаю
окремо»), ручна видача «не за ягоду» і інкасація, яких у системі немає взагалі (§7.3 — список
джерел закритий).

---

## 6. Індекси та інваріанти

Механічні (в схемі):

| де | що |
| --- | --- |
| `shifts` | `UNIQUE (collection_point_id) WHERE closed_at IS NULL` — §7.8, дві відкриті зміни на точці неможливі |
| `cash_counts` | `UNIQUE (shift_id, book, kind) WHERE kind <> 'midday'` — рівно один перерахунок на відкритті й на закритті кожної книги |
| `grade_prices` | `UNIQUE` немає навмисно — історія і є вимогою §4.2 |

Рядків про `UNIQUE (collection_point_id, effective_from)` на `cash_floats`/`crate_allotments`
і про `UNIQUE (payout_id)` на `expected_cash_returns` тут більше немає — усі три таблиці
скасовані (розділ 10, розділи 4.2 і 3). Рядка про `transfers.code` тут не було й раніше:
поля не існує (§4.4).

Домовленості застосунку (схема їх не перевіряє, і це названо, а не сховано):

1. `shifts.explanation` обовʼязкове, коли зміна закривається з розбіжністю ≠ 0.
2. Узгодженість станів `transfers`: `accepted_*` заповнені при `accepted`, `reported_*` — лише
   при `disputed`, `resolved_*` — лише коли спір закрито.
3. Ціна дня в `grade_prices` мусить бути тієї самої точки, що зміна квитанції, — FK цього більше
   не тримає (див. 4.1).

---

## 7. Припущення під відкриті запитання

Схема рухається, ціна названа. Кожне припущення — з посиланням на запитання в
`26-правки-і-запитання.md`.

| запитання | припущення | що ламається, якщо відповідь інша |
| --- | --- | --- |
| **12** — ящики за типом тари | одна одиниця ящика (§12.3 дефолт: ягоду не пересипають); наділ не розбитий за типами | `crate_issuances` потребує `tare_type_id`, `collection_points.target_crates` — розбиття за типами (окремим полем на тип або окремою таблицею), і склад цільового значення 800 = 341 + 195 + 264 перестає бути тотожністю |
| **3** — як зберігається каса | дві книги, обидві на рівні точки; книга ящиків по мережі не зводиться (правка 10) | якщо база теж веде касу — потрібна третя книга і рух між нею й точкою |
| **4** — спірний переказ | число керівника лягає в `resolved_*` того самого документа | якщо розбіжність має бути окремим документом — нова таблиця |
| **11** — заокруглення виплати | схема не змінюється: `payouts.amount` зберігає видане, залишок похідний | нічого в схемі; це правило застосунку |
| **2** — номер розписки | вписується руками (`crate_issuances.receipt_no` уже так) | генерація вимагала б послідовності в межах точки |

---

## 8. Поза обсягом цієї ітерації

Названо, щоб не читалося як повнота: **переважування** (§8.1, 8.2, 8.7), **витрати дня**
(§8.3) з єдиним у системі документом, який можна видаляти, **політика розподілу витрат**
(§8.5, належить парі «день + пункт»), **вечірнє відправлення ящиків** (§6.8),
**налаштування мережі** — межа дод. ціни ±30 ₴/кг і поріг розписки 50 (§2.9, §6.2), **звітність**
(частина 11). Журналу подій не буде за рішенням: §11.5 — «журнал усіх змін: цього в програмі
НЕМАЄ».

**Не полагоджено й лишається відкритим:** схема й далі дозволяє сторнувати квитанцію, на яку
вже лягли розподіли виплат (для ящиків §9.3 таке прямо забороняє), і обидва грошові інваріанти
`payout_allocations` — домовленість, а не `CHECK`.

---

## 9. Що зробити слідом

Список нижче писався вранці 03.09.2026, **до** перегляду власника (розділ 10), і виконаний
уже під переглянутий стан — числа приведені до факту, не до первісного плану.

1. `28-db-schema.dbml` — правки за розділами 3–4, оновити шапку («ціна дня на ЗМІНІ» більше не
   правда) і числа в `28-db-schema.md` (**19 таблиць, 8 enum-ів, 43 зовнішні ключі, 179
   полів** — цифри розділу 10, не первісні 22/48/199, перерахувати FK і поля по файлу, не з
   памʼяті).
2. `28-tables.md` — шість нових розділів, чотири роди таблиць перерахувати, оновити абзац «Чого
   в схемі немає».
3. `28-db-schema.drawio` — **фактично сім рамок, не вісім**: рамки «ЦІЛЬОВІ ЗНАЧЕННЯ» немає
   зовсім, бо `cash_floats` і `crate_allotments` скасовані (розділ 10) і жодної таблиці під
   цю рамку не лишилось — `target_cash`/`target_crates` тепер поля `collection_points`,
   усередині рамки «МЕРЕЖА». «КАСА І ПЕРЕКАЗИ» лишається, але тепер тільки з двох таблиць,
   `cash_counts` і `transfers` (`expected_cash_returns` скасована, розділ 10). Рамку «ЦІНИ
   ЗМІНИ» перейменовано на «ЦІНИ ДНЯ». Три геометричні інваріанти (розділ 7 `28-db-schema.md`)
   стосуються сімох рамок, що лишилися, — зокрема просвіт 1036…1640 у колонці 2.
4. Пояснювальна записка про **дві формули каси** (розділ 5) живе двічі й навмисно: на полотні
   діаграми і розгорнутою `Note` — так само, як записка про борг постачальника. Правити треба
   обидві.
5. `npm run verify` — швидкий рівень; зміни поза `src/`, але `pii` читає відслідковувані `.md`.

---

## 10. Що переглянув власник 03.09.2026

Той самий день, яким датована ця спека, розділився надвоє: уранці рішення про цільові
значення історією й очікуване повернення окремою таблицею (первісні розділи 4.2 і 4.5)
записали як зроблені, а над вечір
власник розібрав саме їх і скасував — три з двадцяти двох запланованих таблиць, ані рядка
коду не торкнувшись: фінальна схема має **19 таблиць**, а не 22. Розділи 2–6 і 9 вище переписані так, щоб було видно **обидва** моменти: що
обрали спершу і чому, що скасували потім і чому. Цей розділ — те саме одним місцем, для
читача, який хоче лише підсумок.

### Причина, власником

> «Наділ це лише показник, на який клієнт орієнтується всередині дня; в історії важливі
> фінансові операції, видачі ящиків тощо, а наділ — орієнтир.»

Тобто цільове значення (наділ каси, наділ ящиків) не належить до того самого класу фактів, що
`payouts`, `crate_issuances` чи `transfers`: воно не подія, яку хтось здійснив у певний
момент, а поточна вказівка, за якою людина звіряє те, що відбувається просто зараз. Зберігати
історію цієї вказівки — означало б лікувати симптом (клієнтка сказала «технологія з грошима
така сама, як з ящиками» — розділ 2) там, де насправді питання інше: чи взагалі хтось коли-
небудь читає це заднім числом.

### Факт, який перевірили перед рішенням

Перед тим як скасовувати, перевірили не аргумент, а **код**: які екрани читають цільове
значення і як саме вони поводяться з датою.

- `src/pages/PointCashPage.tsx` читає його через `workDate` — спільну робочу дату, яку
  гортають стрілками «‹ ›» і яку можна поставити на будь-який минулий день.
- `src/pages/TransfersPage.tsx` читає його через власний локальний стан дати (`React.useState`
  від `workDate`), теж зі стрілками, теж гортаний назад.
- А `src/pages/CratesPage.tsx` — екран «Ящики», де цільове значення власне й показується
  найчастіше, — **не має дати, яку можна погортати**: він прибитий до
  `config.businessToday` і коментар у файлі каже чому дослівно: «Це не звіт, який гортають, а
  стан точки просто зараз... Гортати дні — робота «Журналу», не цього екрана».

Тобто рівно два екрани з трьох, які взагалі торкаються цільового значення, здатні показати
його заднім числом, і жоден із них не показує саме **цільове значення** заднім числом — вони
гортають дату візиту чи переказу, а наділ і касу підтягують під ту дату лише як контекст.
Екран, де наділ — головний предмет погляду, дивиться винятково на сьогодні. Історія
`effective_from` обслуговувала б читача, якого в застосунку не знайшлося.

### Ціна рішення, названа вголос

Без `effective_from` зміна цільового значення діє **відразу на всі дні, включно з минулими**.
Якщо керівник підняв наділ ящиків з 600 до 800 п'ятого числа, а хтось відкриє звірку за перше
число — «не вистачає до цільового» на тому першому числі порахується за **сьогоднішнім** 800,
а не за тим 600, що діяло насправді першого числа. Дата, за яку рахують, не міняє числа,
яким рахують. Це прийнято свідомо: наділ **не гортають назад** — той самий факт, що привів до
скасування, робить і цю ціну прийнятною. Хто захоче іншого — це вже нове рішення, а не
відкат до `effective_from`, бо `effective_from` розвʼязував би геть інше запитання, ніж те,
яке власник поставив під сумнів.
