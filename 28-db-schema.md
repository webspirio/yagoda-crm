# 28 · Стиль схеми БД

Прозова пара до `28-db-schema.drawio`. Тут записано **як саме** та схема намальована, щоб
наступну (або дописану) можна було зробити такою самою, не відновлюючи рішення з памʼяті.

Схема побудована з DBML, надісланого 02.09.2026 (сім таблиць: `collection_points`, `users`,
`shifts`, `suppliers`, `products`, `product_grades`, `shift_product_settings`). Це **не**
`27-entities.dbml` — там інша модель (`text`-ключі, пари дата/час, `berries`, ціна дня в
`price_records`). Плутати їх не можна.

## 1. Сітка

Усе тримається на трьох числах, і решта з них виводиться:

| величина | значення | наслідок |
| --- | --- | --- |
| ширина таблиці `W` | 350 | колонки стоять із кроком 430 → `x = 40, 470, 900` |
| висота заголовка `HDR` | 32 | `висота таблиці = 32 + n × 26` |
| висота рядка `ROW` | 26 | центр *i*-го рядка: `y = yТаблиці + 32 + i×26 + 13` |

Ширина розкладена на три комірки: **34** (ключ) + **160** (назва) + **156** (тип). Стовпчик
ключів вузький навмисно — саме він робить `PK`/`FK` скануванням очима, а не читанням.

Проміжок між колонками — 80 px (`390…470`, `820…900`). Це не порожнеча для краси: там
проходять вертикальні сегменти ліній (розділ 5).

**Смуги.** Таблиці згруповані у горизонтальні смуги з підписом і тонкою лінією під ним;
таблиці однієї смуги вирівняні по спільній верхній лінії. У поточній схемі смуги дві:
`МЕРЕЖА · ПУНКТИ, ЛЮДИ, ЗМІНИ` та `НОМЕНКЛАТУРА · ТОВАРИ, СОРТИ, ЦІНИ ЗМІНИ`.

## 2. Палітра

Колір заголовка = група таблиць, і більше ніщо. Він **не** означає ані тип таблиці, ані
частоту змін.

| група | заголовок | підсвітка ключових рядків | таблиці |
| --- | --- | --- | --- |
| мережа й люди | `#4472c4` | `#e9f0fb` | `collection_points`, `users` |
| зміна та її ціни | `#4f9153` | `#ebf4ea` | `shifts`, `shift_product_settings` |
| постачальники | `#c07b17` | `#fdf2e4` | `suppliers` |
| номенклатура | `#7a5b93` | `#f3edf7` | `products`, `product_grades` |

Тіло таблиці **біле**, не тоноване. Тон лишається валютою: ним підсвічені тільки рядки
ключів, тому вони видно з відстані. Якщо затонувати все — не буде видно нічого.

Службові кольори: роздільник рядків `#d9dde2`, текст назви `#1f2328`, текст типу `#6b7280`,
лінія звʼязку `#8b93a1`, рамка/лінія смуги `#dfe3e8`, нотатка `#fdf8e3` на `#d9c56a`.

## 3. Словник позначень

| знак | значення |
| --- | --- |
| **`PK`** | первинний ключ; назва поля теж **напівжирна** |
| *`FK`* | зовнішній ключ |
| `?` після назви, сірим | поле може бути порожнім (NULL) |
| нічого після назви | поле **обовʼязкове** — це стан за замовчуванням |
| `· UQ` у стовпчику типу | унікальне |
| `= true`, `= 0` | значення за замовчуванням |
| `◇` окремим сірим рядком | складений унікальний індекс |

**`NN` навмисно не пишеться.** Перша редакція схеми ставила `NN` у кожному обовʼязковому
полі — **45 повторень** одного слова на 52 поля, серед яких треба було вишукати **сім**
винятків. Інверсія (мовчання = обовʼязкове, `?` = порожнє) прибирає шум і залишає помітним
саме виняток. Числа зміряні по самому файлу, не з голови.

## 4. Стилі draw.io

Копіювати дослівно; підставляються тільки величини у кутових дужках.

Контейнер таблиці:

```
shape=table;startSize=32;container=1;collapsible=0;childLayout=tableLayout;fixedRows=1;
rowLines=0;fontStyle=1;fontSize=13;fontColor=#ffffff;align=center;verticalAlign=middle;
resizeLast=1;html=1;strokeColor=<заголовок>;fillColor=<заголовок>;
```

Рядок (`fillColor`: `#ffffff` звичайний, `<підсвітка>` для `PK`/`FK`, `#f6f6f6` для індексу):

```
shape=partialRectangle;collapsible=0;dropTarget=0;fillColor=<фон>;strokeColor=#d9dde2;
points=[[0,0.5],[1,0.5]];portConstraint=eastwest;top=0;left=0;right=0;bottom=1;html=1;
```

`points=[[0,0.5],[1,0.5]]` разом із `portConstraint=eastwest` — це те, що дозволяє чіпляти
лінію **до поля**, а не до таблиці, і тільки збоку. Без нього вся розкладка ліній розсиплеться.

Три комірки рядка (спільна частина плюс власна):

```
shape=partialRectangle;connectable=0;fillColor=none;strokeColor=none;overflow=hidden;
html=1;verticalAlign=middle;
  ключ  (w=34):  align=center;fontSize=9;fontColor=<рамка групи | #8a8f98>;
  назва (w=160): align=left;spacingLeft=2;fontSize=11.5;fontColor=#1f2328;
  тип   (w=156): align=left;spacingLeft=4;fontSize=10;fontColor=#6b7280;
```

Рядок індексу — виняток: комірок теж три за формою, але назва й тип **злиті в одну
завширшки 316** (34 + 316 = 350), бо в індексу немає типу; текст курсивом, `#6b7280`.

Звʼязок:

```
edgeStyle=orthogonalEdgeStyle;rounded=0;html=1;
exitX=0;exitY=0.5;exitDx=0;exitDy=0;entryX=<0|1>;entryY=0.5;entryDx=0;entryDy=0;
startArrow=ERmany;startFill=0;endArrow=ERmandOne;endFill=0;
strokeColor=#8b93a1;strokeWidth=1.5;fontSize=10;fontColor=#4b5563;labelBackgroundColor=#ffffff;
```

Нотатка (жовта, пунктирна) і легенда (сіра, суцільна) — той самий стиль із різними кольорами:

```
rounded=1;arcSize=6;whiteSpace=wrap;html=1;align=left;verticalAlign=top;
spacingLeft=12;spacingTop=8;spacingRight=10;fontSize=11;
нотатка: fontColor=#5b4a12;fillColor=#fdf8e3;strokeColor=#d9c56a;dashed=1;
легенда: fontColor=#374151;fillColor=#fbfcfd;strokeColor=#dfe3e8;
```

Виноска від нотатки до поля — ортогональна, пунктирна, **без стрілки**:
`edgeStyle=orthogonalEdgeStyle;rounded=0;endArrow=none;dashed=1;strokeColor=#d9c56a;strokeWidth=1;`

## 5. Лінії — головне правило

Лінії **ортогональні, кути прямі** (`rounded=0`), закінчення — «гусяча лапка»:
`startArrow=ERmany` на боці «багато» (де лежить зовнішній ключ), `endArrow=ERmandOne` на
боці «рівно один». Читається від лапки: *багато змін — в одному пункті*.

Сторона підключення обирається за взаємним положенням таблиць, а не на око:

| де ціль | `exitX` | `entryX` |
| --- | --- | --- |
| ліворуч від джерела | `0` (ліворуч) | `1` (праворуч) |
| у тій самій колонці | `0` | `0` — лінія робить петлю збоку |

**Кожній лінії прописані дві точки маршруту** — `<Array as="points">` з однаковим X
(«коридор») і Y початкового та кінцевого рядка. Це перетворює маршрут із «як вирішить
трасувальник» на «як намальовано»: горизонталь → вертикаль у коридорі → горизонталь.

Коридори розносяться так, щоб **жодні дві вертикалі не лягли одна на одну**. У поточній
схемі використано вісім:

| проміжок | коридори | що ними йде |
| --- | --- | --- |
| `390…470` (колонки A↔B) | 402, 418, 434 | три ключі на `collection_points` |
| той самий | 452 | виноски нотаток (найближче до таблиці — вони другорядні) |
| `820…900` (колонки B↔C) | 840, 860 | `opened_by` / `closed_by` → `users` |
| той самий | 845, 880 | `shift_product_settings` → `product_grades` / `shifts` |
| праворуч від C | 1275 | виноска до `base_price` |

Крок 16–20 px: менше — лінії зливаються, більше — коридори не вміщаються у проміжок.

Дві лінії з `shifts` у `users` підписані (`відкрив`, `закрив`) з
`labelBackgroundColor=#ffffff`: без підпису вони нерозрізненні, без підкладки текст лягає
на лінію.

## 6. Три інваріанти, які треба перевіряти

Стиль тримається не на смаку, а на трьох геометричних умовах. Їх легко порахувати зі
самого `.drawio` (розібрати XML, зібрати прямокутники таблиць і полілінії ребер із заданих
точок):

1. **Жодні дві фігури верхнього рівня не перетинаються** — ані таблиці, ані нотатки, ані легенда.
2. **Жоден сегмент лінії не проходить крізь чужу таблицю чи нотатку** (свої джерело й ціль
   не рахуються).
3. **Жодні дві вертикалі різних ліній не збігаються по X із перекриттям по Y** — інакше дві
   лінії читаються як одна.

Усі три виконуються на поточному файлі. Це саме те, чого око не бачить на схемі з десятком
ліній: перетин на 6 px і накладання двох коридорів виглядають однаково нормально.

## 7. Чого цей стиль не робить

- **Не показує кратність точніше за «багато/один».** `0..*` і `1..*` тут не розрізняються:
  чи обовʼязковий звʼязок, видно лише з `?` біля зовнішнього ключа в таблиці-джерелі.
- **Не показує `ON DELETE` / `ON UPDATE`.** Жодного знака для каскадів немає.
- **Не показує неунікальні індекси.** Рядок `◇` малюється тільки для складених унікальних;
  звичайні індекси на схемі невидимі.
- **Кольорові групи — рішення людини, а не властивість схеми.** Ніщо не заважає віднести
  таблицю не до тієї групи; перевірити це нічим.
- **Коридори не перераховуються самі.** Пересунули таблицю — маршрути лишилися на старих X,
  і три інваріанти розділу 6 доведеться перевірити наново.
