# Ягода CRM — інструкції для агента

Демо-CRM прийомки ягоди: React 19 + Vite, стан у zustand з `persist`, жодного сервера й
жодної бази. Публікується як GitHub Pages під базовим шляхом `/yagoda-crm/`.
Грошовий рушій — `src/lib/calc.ts` (округлення до копійки, FIFO-розподіл виплат,
«Разом» візиту). Мова інтерфейсу, коментарів і цього файлу — українська.

`docs/` і `input/` у `.gitignore` свідомо: там прізвища з неанонімізованого файлу клієнта,
а репозиторій публічний. Не розширювати цей ігнор.

## Як запускати перевірки

**Гейт наприкінці ходу — це ТІЛЬКИ швидкий рівень.** У ньому немає `build`, `smoke` і
`coverage`, отже хід може завершитися зеленим, жодного разу не зібравши застосунок. Це
свідомий компроміс на користь швидкості: `tsc -b` у швидкому рівні є, тому непокритим
залишається саме те, що ламається лише у збірці vite (base path, ассети, CSS).

```bash
npm run verify        # швидкий рівень — те саме, що гейт наприкінці ходу (~6 с)
npm run verify:full   # + build, smoke у Chromium, coverage
npm run verify:ci     # форма CI: пропуск теж вважається падінням
```

Кожна перевірка — окрема команда, яку можна запустити сама (`npm run lint`,
`npm run typecheck`, `npm test`, `npm run deadcode`, …). Нічого не існує лише всередині
оркестратора і нічого — лише всередині CI.

П'ять станів, які ніколи не зливаються в один:

| стан | значення | блокує |
| --- | --- | --- |
| `PASSED` | запустилося, минуло | ні |
| `FAILED` | запустилося, впало | так |
| `SKIPPED` | передумови немає — не було де запускатися | ні, але це треба сказати вголос |
| `NOT_RUN` | залежність не пройшла, тому й не пробували | так |
| `UNRUNNABLE` | команда не змогла стартувати (127 / spawn error) | так |

`UNRUNNABLE` — не педантизм. Написати «lint FAILED», коли лінтера просто немає в `PATH`,
означає стверджувати про код те, чого ніхто не перевіряв.

## Що кожна перевірка доводить і чого не доводить

<!-- BEGIN:verify-table -->
<!-- Згенеровано з scripts/verify/registry.mjs. Руками не правити:
     перевірка `memo` порівнює цей блок побайтово і падає на розбіжності.
     Щоб змінити текст — правити registry.mjs, потім
     `node scripts/verify/checks/memo-drift.mjs --write`. -->

| перевірка | рівень | що доводить | чого НЕ доводить |
| --- | --- | --- | --- |
| `lint` | fast | oxlint розібрав кожен файл, що потрапляє під його конфіг, і не залишив ані помилки, ані попередження за ввімкненими правилами — бо --max-warnings=0 робить warn і error одним. | Нічого про поведінку: чи арифметика правильна, чи компонент рендериться. Правило, яке не ввімкнене, для нього не існує — а ввімкнений набір це, по суті, типова категорія correctness плюс два явні правила, тож про залежності хуків він каже мало. Виняток у .oxlintrc.json перелічує РІВНО ТРИ файли поштучно (button.tsx, badge.tsx, tabs.tsx), не глоб ui/**, і саме там ховаються три знахідки. |
| `typecheck` | fast | tsc -b звів усі три проєкти (app, node, scripts) без помилок: типові контракти між модулями узгоджені, немає невикористаних локальних змінних і параметрів, немає провалу через case. Разом зі scripts/ це покриває і код самого шару перевірок. | Нічого про рантайм-дані. ISODate = string приймає "hello"; кожен ! (напр. App.tsx:25) і кожен as — дірка. Про JSON, що приходить з localStorage, tsc не знає нічого. |
| `test` | fast | Обрані сценарії над src/lib (calc, seed, visit) пройшли свої assertions: округлення до копійки, FIFO-розподіл виплат, «Разом» візиту, зведення дня. | Жоден .tsx не виконується цим рядком: у репозиторії їх 45 (20 поза src/components/ui/, і 19 потрапляють у звіт покриття). Усі сторінки й компоненти поза ним. Нічого про сценарії, яких ніхто не написав, і нічого про силу assertions: тест, що викликає функцію і не перевіряє результат, зелений так само. |
| `testfiles` | fast | Кожен файл *.{test,spec}.* у репозиторії підбирається рівно одним runner-ом — vitest або playwright, не нулем і не двома. | Нічого про самі тести. Лише про те, що жоден файл не втрачено між двома glob-ами — саме та перевірка, якої не було, коли .tsx-тести могли б лежати непідібраними. |
| `ratchet:money` | fast (після: typecheck) | Кожен арифметичний вираз, що потрапляє у uah або uahAuto, обгорнутий round2()/sum(), окрім перелічених у baselines/money-rounding.json — з точною кількістю входжень, тому часткове виправлення не зараховується. Прив’язки форматерів і round2/sum розібрані по імпортах, тому ні псевдонім, ні namespace-імпорт, ні локальний самозваний round2 не проходять. | Нічого про правильність самої арифметики: round2(a*b) перевірено на округлення, не на сенс. Під храповиком лише uah/uahAuto — num/kg/tonnage навмисно виведені (на них правило давало переважно шум) і тільки друкуються у звіті, тому ₴-величина, показана через num(), НЕ гарантована. І перевіряється арифметика САМЕ в місці виклику форматера: значення, пораховане у змінну, невидиме — і row.net += r.net (DashboardPage.tsx:66-68), і avgPrice = totals.amount / totals.net (:86), який друкується як ₴/кг на :201. |
| `ratchet:persist` | fast (після: typecheck) | Кожен ключ, який store.ts віддає у partialize, має рантайм-звуження на шляху rehydrate, окрім перелічених у baselines/persist-boundary.json — і кожен запис того baseline досі відтворюється. | Не перевіряє глибину: Array.isArray(receptions) зараховується, хоча про поля всередині не каже нічого. Зараховуються лише звуження, семантику яких видно з AST (Array.isArray, typeof, instanceof, in, схема .parse, локальний type predicate) — предикат, імпортований з іншого файлу, підтвердити неможливо і він НЕ зараховується. Деструктурований параметр rehydrate-колбека теж не відслідковується: справжня перевірка через ({ receptions }) буде показана як відсутня. Лише цей один store. |
| `deadcode` | fast (після: typecheck) | Набір невикористаних файлів, експортів і залежностей дорівнює baselines/dead-exports.json, у обидва боки: нова знахідка падає, зникла падає теж. Плюс конфіг knip містить ЛИШЕ entry/project (усе інше — поза дозволеним списком), іншого конфігу knip немає, глоби зафіксовані відпечатком у baseline, і в коді немає тегів @public/@internal, якими можна прибрати знахідку без зміни конфігу. | Роздільна здатність самого knip: динамічний доступ obj[name], рядкові ключі, усе, до чого дістаються лише з HTML. Глоби project покривають тільки .ts/.tsx/.mjs, тому мертві не-кодові файли (напр. src/assets/*.png, public/icons.svg) невидимі для нього у принципі. І запис baseline з неправильною причиною проходить так само, як з правильною — причини перевіряє людина, не чекер. |
| `memo` | fast | Таблиця «доводить / не доводить» у CLAUDE.md збігається символ у символ з тим, що рендерить цей registry, плюс контрольна сума над СИРИМИ рядками registry — тобто ані пам’ятка не могла відстати від коду, ані правка registry не могла сховатися у згорнутих пробілах. | Порівнює лише цю таблицю. Про решту CLAUDE.md і про те, чи самі формулювання правдиві, не каже нічого: збіг двох артефактів — не доказ, що обидва праві. |
| `lint:exempt` | fast | Список винятків react/only-export-components у .oxlintrc.json дорівнює baselines/lint-exempt.json — і зі знятим винятком кожен із тих файлів дає РІВНО ту кількість знахідок, що записана. Тобто виняток може лише зменшуватися, і виняток на весь файл більше не ховає нових знахідок у ньому. | Лише це одне правило і лише механізм overrides. Про рядкові директиви oxlint-disable-next-line не каже нічого — їх ніщо не храповить, вони тримаються тільки на вимозі писати причину в тому ж рядку. |
| `pii` | fast | docs/ і input/ досі ігноруються git, жоден відслідковуваний файл не лежить під ними, і жодної таблиці (.xlsx/.xls/.numbers) не відслідковується. Це межа, за якою 208 справжніх прізвищ клієнта не потрапляють у публічний репозиторій. | Перевіряє МЕЖУ, не ВМІСТ: жодного сканування відслідковуваних файлів на прізвища немає, тому прізвище, вписане прямо в src/, ця перевірка не побачить. Історію git вона теж не читає — якщо щось уже було закомічено раніше, тут буде зелено. |
| `build` | full | tsc -b пройшов і vite зібрав dist/ з index.html та assets. | Нічого про те, чи артефакт працює, і нічого про базовий шлях: змініть base на /wrong/ — ця команда все одно вийде з нулем. Базовий шлях доводить лише smoke, бо він відкриває артефакт саме під /yagoda-crm/. Збірка може пройти, а застосунок — упасти на першому рендері. |
| `smoke` | full (потребує: playwright-browser) (після: build) | Зібраний артефакт, поданий під своїм продакшн-базовим шляхом, піднявся у Chromium без page error і без console error; один візит пройшов від ваги до «Разом» із перевіркою самої суми (не лише підпису) і без «NaN» у тексті сторінки; усі розділи навігації та роль власника відкрилися так само чисто. | Один браузер, один розмір вікна, тільки демо-дані з seed — і, головне, шлях rehydrate НЕ виконується жодного разу: браузер починає з порожнім localStorage, тому саме та межа, яку описує ratchet:persist, тут не торкається. Перевіряється рендер, відсутність помилок і відсутність NaN, а не правильність жодної суми. Retries = 0, тому нестабільність видно як червоне, а не як «пройшло з другої спроби». |
| `coverage` | full | Покриття рядків і гілок для src/** (крім src/components/ui/**, src/main.tsx і самих тестів) зміряно і записано у coverage/. Локально це і все, що воно доводить. У CI пороги беруться з env у .github/workflows/verify.yml, і відсутнє або нечислове значення тепер валить перевірку, а не тихо стає нулем. | Виконання — не перевірка. Тест, який викликає функцію і нічого не стверджує, підіймає покриття так само, як справжній. Поріг для .tsx — 0: число показане, але нічого не вимагає, бо компонентних тестів немає жодного. |

<!-- registry-checksum: ed95527c5cb0788c1d277c157a0ed398 -->
<!-- END:verify-table -->

### Свідомі рішення, які легко «полагодити» назад

- **jsdom і @testing-library НЕ встановлені, і це навмисно.** Другий тестовий стек із
  нулем тестів у ньому був би мертвою залежністю, яку `deadcode` справедливо б і показав.
  Якщо колись знадобляться компонентні тести — ставити стек разом із першим справжнім
  тестом, не раніше.
  **Але не читати це як «компонентний рівень покрито».** Playwright відкриває сторінки у
  справжньому браузері й перевіряє, що вони рендеряться без помилок і без NaN — це не
  покриття рядків і не перевірка жодної суми. Покриття `.tsx` зміряне й дорівнює **0,00 %
  (0 з 868 рядків)**; e2e-прогін нічим не інструментований.
- **`npm run preview` містить `--base /yagoda-crm/`.** Без цього прапорця preview віддає
  dist із кореня, а index.html просить `/yagoda-crm/assets/…` — усі ассети 404 і біла
  сторінка. `vite.config.ts` виставляє base лише для `command === 'build'`, а preview
  працює як `serve`. Playwright спеціально запускає саме `npm run preview`, щоб бачити
  той самий артефакт, що й людина.

Порогів покриття локально немає (підлога 0) — вони живуть лише в
`.github/workflows/verify.yml`, там де їх застосовують. Зниження порога — видима правка
саме в тому файлі.

### CI: лампочка чи гейт

`verify.yml` існує і проходить (перший зелений прогін на раннері GitHub — 13/13, 1 хв 16 с).
Але **CI стає гейтом лише тоді, коли для `main` увімкнено required status check**. Це
налаштування репозиторію, не код, тож із самого коду його не видно і жодна перевірка тут
його не забезпечує.

Станом на 2026-08-23 воно **НЕ ввімкнене**: `branches/main/protection` віддає 404,
`rulesets` — `[]`. Тобто зараз червоний `verify` нікого не спиняє від мерджу в `main`.
Єдине, що вже тримається механічно, — `deploy` має `needs: verify`, тому червоне дерево
не поїде на Pages (сайт просто лишиться на попередній збірці).

Увімкнути (потрібні адмінські права на репозиторій; `gh` на цій машині авторизований як
`swefd`, який є адміном в організації `webspirio`):

```bash
gh api -X PUT repos/webspirio/yagoda-crm/branches/main/protection \
  -F 'required_status_checks[strict]=true' \
  -F 'required_status_checks[contexts][]=verify' \
  -F 'enforce_admins=false' \
  -F 'required_pull_request_reviews=' -F 'restrictions='
```

Перевірити, що подіяло:

```bash
gh api repos/webspirio/yagoda-crm/branches/main/protection \
  --jq '.required_status_checks | {strict, contexts}'
```

Що варто знати перед тим, як вмикати:

- `contexts` мусить містити саме `verify` — так називається job у `verify.yml`. Якщо job
  колись перейменують, захист почне вимагати перевірку, якої більше немає, і мерджі
  застрягнуть назавжди.
- `strict=true` означає, що гілку треба оновити від `main` перед мерджем. Це саме те, що
  закриває дірку «обидві гілки зелені окремо, а разом ламаються».
- `enforce_admins=false` лишає адміну можливість пропхати правку в обхід — свідомо, щоб
  зламаний `verify` не заблокував і той мердж, який його лагодить. Якщо потрібна
  безумовна дисципліна — поставити `true`.
- Вмикати **після** того, як `verify` хоч раз пройшов на реальному прогоні. Вимагати
  перевірку, яка ще ніколи не була зеленою, означає заблокувати всі мерджі, включно з
  виправленням.
- Захист гілки також забороняє прямий `git push` у `main` — працювати доведеться через PR.

## Три правила

### 1. Докази під заявою

**Цю таблицю не перевіряє ніщо.** Гейт наприкінці ходу завжди запускає лише швидкий
рівень; він не дивиться, що саме змінилося, і не вимагає відповідних доказів. Тобто нижче
— зобовʼязання, а не механізм. Механічно тримаються тільки п'ять станів, храповики
baseline, заборона ключів приглушення в `knip.json`, храповик винятків oxlint і пороги
покриття в CI.

Не «зроблено», а «зроблено, перевірено тим-і-тим». Відповідність така:

| що змінилося | які докази обов'язкові |
| --- | --- |
| будь-що в `src/` | `npm run verify` (швидкий рівень) |
| `src/lib/calc.ts`, `store.ts`, `seed.ts` | `npm run verify` + `npm run coverage`, і назвати цифру |
| будь-який `.tsx` | `npm run verify:full` — бо саме smoke, а не unit-тести, торкається компонентів |
| `vite.config.ts`, `index.html`, `public/` | `npm run verify:full` (base path ламається лише в збірці) |
| `package.json`, лок-файл | `npm run verify:full` + `npm run deadcode` |
| будь-що в `scripts/verify/`, `.claude/hooks/` | `npm run verify` — цей код теж під `tsc -b` |
| registry перевірок | `node scripts/verify/checks/memo-drift.mjs --write`, інакше `memo` впаде |
| `.oxlintrc.json` overrides | `npm run lint:exempt` — і оновити `baselines/lint-exempt.json` |
| `.gitignore`, будь-що біля `docs/` та `input/` | `npm run pii` — межа публічного репозиторію |

Заявляти успіх без прочитаного виводу не можна. Якщо перевірка не запускалася — так і
казати, а не «мабуть, зелено».

### 2. Пропуски проговорюються вголос

«Швидкий рівень зелений; build і smoke пропущено — немає браузера» — і ніколи
«все зелено». Пропуск означає, що про цю частину не перевірено нічого; це інформація, а
не дрібниця, яку можна округлити до успіху.

### 3. Храповики крутяться лише в один бік

Усі baseline у `scripts/verify/baselines/` можуть тільки зменшуватися. Правило
двостороннє: нова знахідка падає, і знахідка, якої вже немає, теж падає — запис треба
прибрати. Причина в кожному записі обов'язкова, і чекер сам відкидає заглушки
(`TODO`, порожній рядок, коротше за 30 символів).

**Позеленіти, розширивши виняток, — це не позеленіти.** Це найзручніша з доступних
раціоналізацій, тому вона написана тут явно. Якщо перевірка червона, а рука тягнеться
дописати запис у baseline, послабити правило oxlint, знизити поріг покриття або додати
ключ приглушення в `knip.json` — це саме та поломка, від якої цей шар і будували.
`deadcode` навіть падає, якщо в `knip.json` з'явився будь-який ключ приглушення.

Виняток дозволений лише коли він: перелічений поштучно, датований, з причиною, яку
перевірено проти джерела, і сам себе скасовує, коли знахідка зникає.
