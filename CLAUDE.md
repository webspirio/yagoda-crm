# Ягода CRM — інструкції для агента

Демо-CRM прийомки ягоди: React 19 + Vite, стан у zustand з `persist`, жодного сервера й
жодної бази. Публікується як GitHub Pages під базовим шляхом `/yagoda-crm/`.
Грошовий рушій — `src/lib/calc.ts` (округлення до копійки, FIFO-розподіл виплат,
«Разом» візиту). Мова інтерфейсу, коментарів і цього файлу — українська.

`docs/` і `input/` у `.gitignore` свідомо: там прізвища з неанонімізованого файлу клієнта,
а репозиторій публічний. Не розширювати цей ігнор.

**Третій запис — `.superpowers/` (додано 28.08.2026, коміт `10dcc0f`).** Це робочий каталог
агента: план фази, брифи задач, звіти й реєстр рішень. Він поза git СВІДОМО — це чернетки
процесу, а не код і не документ клієнта, — але ціна названа вголос: увесь робочий запис
фази 4 (план, три звіти задач, реєстр рішень і фінальна рецензія гілки) лишається поза тим
самим репозиторієм, у якому решта фази задокументована. Хто читатиме історію з чистого
клону, побачить коміти без жодного сліду того, ЧОМУ вони такі. Рішення, які мусять пережити
цей каталог, переносяться сюди або у `docs/` окремим абзацом — саме так зроблено з цим.
Межі `pii` це не зсуває: `docs/` і `input/` лишаються рядками 16 і 25 того ж файлу.

## Як запускати перевірки

**Гейт наприкінці ходу — це ТІЛЬКИ швидкий рівень.** У ньому немає `build`, `smoke` і
`coverage`, отже хід може завершитися зеленим, жодного разу не зібравши застосунок. Це
свідомий компроміс на користь швидкості: `tsc -b` у швидкому рівні є, тому непокритим
залишається саме те, що ламається лише у збірці vite (base path, ассети, CSS).

```bash
npm run verify        # швидкий рівень — те саме, що гейт наприкінці ходу (~6 с)
npm run verify:full   # + build, smoke у Chromium, coverage
npm run verify:ci     # форма CI: пропуск теж вважається падінням
```

Кожна перевірка — окрема команда, яку можна запустити сама (`npm run lint`,
`npm run typecheck`, `npm test`, `npm run deadcode`, …). Нічого не існує лише всередині
оркестратора і нічого — лише всередині CI.

П'ять станів, які ніколи не зливаються в один:

| стан | значення | блокує |
| --- | --- | --- |
| `PASSED` | запустилося, минуло | ні |
| `FAILED` | запустилося, впало | так |
| `SKIPPED` | передумови немає — не було де запускатися | ні, але це треба сказати вголос |
| `NOT_RUN` | залежність не пройшла, тому й не пробували | так |
| `UNRUNNABLE` | команда не змогла стартувати (127 / spawn error) | так |

`UNRUNNABLE` — не педантизм. Написати «lint FAILED», коли лінтера просто немає в `PATH`,
означає стверджувати про код те, чого ніхто не перевіряв.

## Що кожна перевірка доводить і чого не доводить

<!-- BEGIN:verify-table -->
<!-- Згенеровано з scripts/verify/registry.mjs. Руками не правити:
     перевірка `memo` порівнює цей блок побайтово і падає на розбіжності.
     Щоб змінити текст — правити registry.mjs, потім
     `node scripts/verify/checks/memo-drift.mjs --write`. -->

| перевірка | рівень | що доводить | чого НЕ доводить |
| --- | --- | --- | --- |
| `lint` | fast | oxlint розібрав кожен файл, що потрапляє під його конфіг, і не залишив ані помилки, ані попередження за ввімкненими правилами — бо --max-warnings=0 робить warn і error одним. | Нічого про поведінку: чи арифметика правильна, чи компонент рендериться. Правило, яке не ввімкнене, для нього не існує — а ввімкнений набір це, по суті, типова категорія correctness плюс два явні правила, тож про залежності хуків він каже мало. Виняток у .oxlintrc.json перелічує РІВНО ТРИ файли поштучно (button.tsx, badge.tsx, tabs.tsx), не глоб ui/**, і саме там ховаються три знахідки. |
| `typecheck` | fast | tsc -b звів усі три проєкти (app, node, scripts) без помилок: типові контракти між модулями узгоджені, немає невикористаних локальних змінних і параметрів, немає провалу через case. Разом зі scripts/ це покриває і код самого шару перевірок. | Нічого про рантайм-дані. ISODate = string приймає "hello"; кожен ! (напр. App.tsx:25) і кожен as — дірка. Про JSON, що приходить з localStorage, tsc не знає нічого. |
| `test` | fast | Обрані сценарії над src/lib пройшли свої assertions — ПЕРЕМІРЯНО 28.08.2026 після фінальної рецензії гілки: 577 тестів у 17 файлах (було 574, до фази 4 — 553 у 15), з яких 16 лежать у src/lib, а сімнадцятий — src/components/crates/helpers.test.ts, тобто НЕ в src/lib. Предмет: округлення до копійки, FIFO-розподіл виплат, «Разом» візиту, зведення дня, наділ і баланс ящиків, каса точки як підзвіт, те, що реєстр users відтворює OPERATORS поточково, а base і all власних записів НЕ мають (це слабше за колишнє твердження про signerFor(): toContain замість toBe, і для base/all вимога протилежна — порожній список замість імені керівника; сама функція видалена 28.08.2026), а з фази 4 — правила сесії (auth.test.ts, 14 тестів: sessionUser, actorName, roleOf, canActOnPoint, scopeAfterSignIn) і мок-«сервер» (auth-mock.test.ts, 6 тестів: три причини відмови поодинці, регістр логіна, пробіли в секреті). І з 28.08.2026 — ГЕЙТ ТОЧКИ у самому сторі: приймальник Шипинок отримує undefined від issueCrates і addVisit, адресованих Гайовому, тоді як той самий виклик під керівником проходить (контрольний виклик у кожному з двох тестів). Доти цього не перевіряв ЖОДЕН тест: заміна actorAt(st, pointId) на actorOf(st) у девʼятьох командах лишала дерево зеленим — перевірено, обидва тести на послабленому сторі падають. | Жоден .tsx не виконується цим рядком: ПЕРЕМІРЯНО 28.08.2026 — у репозиторії їх 68 (43 поза src/components/ui/). Попереднє «66 (41)» було неправдою вже на c4bae16, де їх було 67 і 42: рядок відстав на файл ще до цієї фази. Усі сторінки й компоненти поза ним — включно з екраном входу: те, що кнопка «Увійти» кличе саме signIn(), а не малює свій власний вхід, тут не перевіряє ніщо (це робить smoke). Так само гейт «Прийняти» на прийомці: він викликає checkBerryPayout(), і сама та функція протестована, але те, що кнопка викликає саме її, не перевіряє ніщо. Нічого про сценарії, яких ніхто не написав, і нічого про силу assertions: тест, що викликає функцію і не перевіряє результат, зелений так само. |
| `testfiles` | fast | Кожен файл *.{test,spec}.* у репозиторії підбирається рівно одним runner-ом — vitest або playwright, не нулем і не двома. | Нічого про самі тести. Лише про те, що жоден файл не втрачено між двома glob-ами — саме та перевірка, якої не було, коли .tsx-тести могли б лежати непідібраними. |
| `ratchet:money` | fast (після: typecheck) | Кожен арифметичний вираз, що потрапляє у uah або uahAuto, обгорнутий round2()/sum(), окрім перелічених у baselines/money-rounding.json — з точною кількістю входжень, тому часткове виправлення не зараховується. Прив’язки форматерів і round2/sum розібрані по імпортах, тому ні псевдонім, ні namespace-імпорт, ні локальний самозваний round2 не проходять. | Нічого про правильність самої арифметики: round2(a*b) перевірено на округлення, не на сенс. Під храповиком лише uah/uahAuto — num/kg/tonnage навмисно виведені (на них правило давало переважно шум) і тільки друкуються у звіті, тому ₴-величина, показана через num(), НЕ гарантована. І перевіряється арифметика САМЕ в місці виклику форматера: значення, пораховане у змінну, невидиме — і row.net += r.net (DashboardPage.tsx:66-68), і avgPrice = totals.amount / totals.net (:86), який друкується як ₴/кг на :201. |
| `ratchet:persist` | fast (після: typecheck) | Кожен ключ, який store.ts віддає у partialize, має рантайм-звуження на шляху rehydrate, окрім перелічених у baselines/persist-boundary.json — і кожен запис того baseline досі відтворюється. ПЕРЕМІРЯНО 28.08.2026: у partialize 21 ключ, звужено 21 з 21, а в baseline НУЛЬ винятків — храповику зараз нічого прощати, і будь-який новий ключ без звуження падає одразу. Фаза 4 замінила ключ role на session, не змінивши числа: роль більше не зберігається взагалі, бо вона властивість людини в реєстрі, а не пристрою, — і новий ключ довелося звузити одразу, інакше цей рядок став би червоним. | Не перевіряє глибину: Array.isArray(receptions) зараховується, хоча про поля всередині не каже нічого. І не перевіряє КІЛЬКІСТЬ звужень на ключі — ключ зараховано, щойно видно перше. На session їх ДВА, і друге важливіше: форма (локальний предикат isSession) і ІСНУВАННЯ (userIds.has(p.session.userId) — сесія на userId, якого немає в реєстрі, це не сесія, 23 §Р4-6). Цей рядок бачить лише перше: зміряно 28.08.2026 — з видаленим userIds.has(...) перевірка лишається ЗЕЛЕНОЮ, тобто друге звуження тримають юніт-тести store.test.ts, а не храповик. Зараховуються лише звуження, семантику яких видно з AST (Array.isArray, typeof, instanceof, in, схема .parse, локальний type predicate) — предикат, імпортований з іншого файлу, підтвердити неможливо і він НЕ зараховується. Деструктурований параметр rehydrate-колбека теж не відслідковується: справжня перевірка через ({ receptions }) буде показана як відсутня. Лише цей один store. |
| `deadcode` | fast (після: typecheck) | Набір невикористаних файлів, експортів і залежностей дорівнює baselines/dead-exports.json, у обидва боки: нова знахідка падає, зникла падає теж. Плюс конфіг knip містить ЛИШЕ entry/project (усе інше — поза дозволеним списком), іншого конфігу knip немає, глоби зафіксовані відпечатком у baseline, і в коді немає тегів @public/@internal, якими можна прибрати знахідку без зміни конфігу. ПЕРЕМІРЯНО 28.08.2026 (entries.length, не з памʼяті): у baseline 43 записи — export 32, file 9, dependency 2, і знахідок типу type НЕМАЄ ЖОДНОЇ. Попереднє «45 (export 33, file 9, dependency 2, type 1)» було неправдою двічі: і числом (на c4bae16 їх було 44), і категорією (type не траплявся взагалі). До фаз 5-6 записів було 47, і храповик щоразу не давав лишити мертвий: останній так прибраний — format.ts:initials, який ожив, коли шапка почала малювати ініціали людини з сесії замість зашитих «ВЛ»/«ПР». | Роздільна здатність самого knip: динамічний доступ obj[name], рядкові ключі, усе, до чого дістаються лише з HTML. Глоби project покривають тільки .ts/.tsx/.mjs, тому мертві не-кодові файли (напр. src/assets/*.png, public/icons.svg) невидимі для нього у принципі. І запис baseline з неправильною причиною проходить так само, як з правильною — причини перевіряє людина, не чекер. |
| `memo` | fast | Таблиця «доводить / не доводить» у CLAUDE.md збігається символ у символ з тим, що рендерить цей registry, плюс контрольна сума над СИРИМИ рядками registry — тобто ані пам’ятка не могла відстати від коду, ані правка registry не могла сховатися у згорнутих пробілах. | Порівнює лише цю таблицю. Про решту CLAUDE.md і про те, чи самі формулювання правдиві, не каже нічого: збіг двох артефактів — не доказ, що обидва праві. |
| `lint:exempt` | fast | Список винятків react/only-export-components у .oxlintrc.json дорівнює baselines/lint-exempt.json — і зі знятим винятком кожен із тих файлів дає РІВНО ту кількість знахідок, що записана. Тобто виняток може лише зменшуватися, і виняток на весь файл більше не ховає нових знахідок у ньому. | Лише це одне правило і лише механізм overrides. Про рядкові директиви oxlint-disable-next-line не каже нічого — їх ніщо не храповить, вони тримаються тільки на вимозі писати причину в тому ж рядку. |
| `seed:port` | fast | Жоден файл під src/ не імпортує ані фікстуру демо-даних (`seed`), ані мок-«сервер» логіна й пароля (`auth-mock`) — у жодній формі: аліас, відносний шлях, `import type`, динамічний `import()`. Дозволені перелічені поштучно і на кожен порт свої: seed — store.ts, seed.ts, seed-suppliers.ts; auth-mock — сам лише store.ts. Тобто центральна обіцянка ports.ts («сторінки читають лише DomainSnapshot і Queries») тепер МЕХАНІЗМ, а не зобовʼязання: 27.08.2026 вона була неправдою у 24 з 33 файлів UI, і жодна перевірка цього не бачила. Обидві межі перевірені НА ПАДІННЯ дописаним імпортом у DebtsPage.tsx (28.08.2026), а не оглядом: механізм, який ніхто не пробував зламати, — це обіцянка. Читається РОБОЧЕ ДЕРЕВО, не індекс, тому імпорт, дописаний просто зараз, уже червоний. | Лише ДВА модулі і лише за формою рядка імпорту. Про інші приховані порти не каже нічого: третій модуль з константами застосунку в src/lib пройде без слова. Не бачить реекспорту — ані `seed.ts`, ані `auth-mock.ts`, витягнутий через третій файл, який сам дозволений, невидимий. ЦЯ ДІРКА ВІДКРИТА ЯК БУЛА: фаза 4 нею просто не скористалася (store.ts не реекспортує authenticate), і тримає це рішення огляд, а не цей рядок. Тестові файли не перевіряються взагалі — інакше перевірка забороняла б seed.test.ts і auth-mock.test.ts, тобто те єдине, що ці два модулі й тримає. І нічого не каже про те, чи ПРАВИЛЬНО екран читає config/users/products — лише про те, що він не читає їх із фікстури; так само нічого про те, що секрет не приїхав на екран якимось іншим шляхом, ніж рядок імпорту. Дозвільні списки перевіряє людина, не чекер. |
| `pii` | fast | МЕЖА: docs/ і input/ досі ігноруються git, жоден відслідковуваний файл не лежить під ними, і жодної таблиці (.xls/.xlsx/.xlsm/.numbers) не відслідковується — саме так 208 справжніх прізвищ клієнта не потрапляють у публічний репозиторій. І ВМІСТ: кожен відслідковуваний .ts/.tsx/.js/.mjs/.json/.md/.html/.css/.yml/.yaml/.svg (крім лок-файла й самого чекера) читається рядок за рядком на телефон, e-mail та IBAN; а там, де на машині лежить input/data-example.xlsx, усі імена форми «два-три слова з великої» з відслідковуваних .ts/.tsx/.json/.md звіряються з іменами з файлу клієнта, і перетин валить перевірку, не друкуючи самих імен у лог. | Вміст перевіряється лише за ФОРМОЮ: імена ловить регулярка «два-три слова з великої кирилицею», тому прізвище в транслітерації, в іншій формі або всередині довшого рядка не буде знайдене — це скрипт і сам друкує окремим рядком УВАГА. Звірка з реальним реєстром іде ТІЛЬКИ там, де є input/data-example.xlsx: без нього (як у CI) перевірка каже «input/ відсутній — звірку з реальним реєстром НЕ проводили» і проходить, а якщо в PATH немає unzip — так само тихо звіряє з порожнім реєстром і пише «звірено». Звірка охоплює лише .ts/.tsx/.json/.md, тому ім’я у .yml/.html/.svg/.mjs проти реєстру не перевіряється взагалі. Історію git вона не читає — якщо щось уже було закомічено раніше, тут буде зелено. |
| `audit` | full (потребує: npm-registry) | Набір адвайзорі npm audit дорівнює baselines/audit.json, у обидва боки. І понад те: жодної адвайзорі немає у ПРОДАКШН-дереві залежностей і жодної немає з рівнем critical — ці два класи не заносяться у baseline за жодною причиною. | Вердикт залежить від бази GitHub, яка змінюється без змін тут — тому це повний рівень, а не швидкий: подія на боці не має права заблокувати хід. Нічого не каже про вразливості, яких у базі ще немає, і нічого про власний код: це аудит залежностей, не застосунку. |
| `build` | full | tsc -b пройшов і vite зібрав dist/ з index.html та assets. | Нічого про те, чи артефакт працює, і нічого про базовий шлях: змініть base на /wrong/ — ця команда все одно вийде з нулем. Базовий шлях доводить лише smoke, бо він відкриває артефакт саме під /yagoda-crm/. Збірка може пройти, а застосунок — упасти на першому рендері. |
| `bundle` | full (після: build) | Розмір зібраного dist/assets не перевищує стелю з baselines/bundle-budget.json — і в gzip (те, що переїжджає мережу), і в сирому вигляді (те, що телефон розбирає). Стеля зміряна й округлена вгору кроком 5 KiB (gzip) і 20 KiB (raw), а запас, записаний числом у тому ж файлі (25 839 байтів gzip і 93 324 raw), — це мірка 27.08.2026, а не сьогоднішній стан: справжній запас перевірка друкує на КОЖНОМУ зеленому прогоні, і 28.08.2026, після виправлень за фінальною рецензією гілки, він уже 23 654 байти gzip і 85 592 raw. | Каже лише про байти, і лише про dist/assets. Нічого про час до першого рендеру, про шрифти з Google Fonts, які тягне index.html, і нічого про те, чи цей розмір узагалі виправданий. Головне ж: він міряє СУМУ і не каже нічого про те, як вона розподілена по чанках — а телефон на точці тягне не суму, а перший чанк. ПЕРЕМІРЯНО 28.08.2026 ПІСЛЯ ВИПРАВЛЕНЬ ЗА ФІНАЛЬНОЮ РЕЦЕНЗІЄЮ (попередня мірка була від 27.08: 334,8 KiB gzip проти теперішніх 336,9 — відтоді додався екран входу з сесією в сторі, а сьогодні ще й дві видимі відмови на SettleDialog і CostOfDayPage): у dist/assets 14 файлів (пʼять спільних і девʼять ленивих екранів), разом 336,9 KiB gzip (344 986 байтів) і 1 196,4 KiB raw (1 225 128), з них перший чанк 136,2 KiB gzip, DashboardPage 103,4, спільний bits 38,4 і css 16,5. Те, що приймальник на точці реально тягне на першому відкритті (index + bits + css + es2015 + react-dom), — 202,8 KiB gzip (207 687 байтів), тобто 60 % суми; «Перекази» (5,9) у це не входять, бо ленивий чанк керівника. Екран входу окремим чанком НЕ поїхав навмисно (App.tsx:8-10): без нього застосунок не відкривається взагалі, тому платити за нього другим запитом нема за що — і саме тому весь приріст фази 4 (2 185 байтів gzip) лежить майже цілком у тому, що приймальник тягне на першому відкритті: 200,9 -> 202,8 KiB. І ЩЕ 208,8 KiB ШРИФТІВ з fonts.gstatic.com на тому самому першому відкритті — девʼять файлів, які цей рядок не бачить у принципі, бо міряє лише dist/assets. Тобто стеля стереже менше половини того, що приймальник справді качає (число шрифтів — з мережевої панелі браузера 27.08.2026, і воно досі чинне: index.html відтоді не змінювався жодним байтом). Байти, які переїхали з ленивого чанка у перший, цю перевірку проходять без слова, поки сума в межах — і навпаки, розділення чанків суму НЕ зменшує (на межах навіть додає), тому «бюджет зелений» не означає «на точці стало легше». А з 27.08.2026 вона ловить іще менше: стелю ПІДНЯТО НАПЕРЕД на прохання власника, коли перевірка була зелена, і запас виріс 5 003 -> 25 839 байтів gzip (7 415 -> 93 324 raw). Отже приблизно ціла фаза роботи тепер пройде тут без жодного слова — видно її буде лише як спадання запасу у виводі, а не як падіння: фаза 4 разом із виправленнями за рецензією забрала з нього 2 185 байтів gzip і 7 732 raw, і жодного слова про це рядок у виводі справді не сказав — він лишився зеленим. |
| `smoke` | full (потребує: playwright-browser) (після: build) | Зібраний артефакт, поданий під своїм продакшн-базовим шляхом, піднявся у Chromium без page error і без console error, і ПЕРШИМ його виглядом є екран «Вхід» — окремий assert, який падає, якщо гейт App.tsx послабити до `if (!session)` або прибрати. Далі всі чотири тести проходять справжній вхід: логін у #signin-login, пароль у #signin-secret, кнопка «Увійти» — тобто з фази 4 жоден екран цього файлу не відкривається без сесії. Один візит пройшов від ваги до «Разом» із перевіркою самої суми (не лише підпису) і без «NaN» у тексті сторінки, а в його квитанції рядок «Приймав» дорівнює «Оксана Г.» — імені з облікового запису p1, а не назві точки: це єдине місце, де перевіряється весь ланцюжок вхід -> сесія -> підпис у збереженому документі. Далі РІВНО ТРИНАДЦЯТЬ відкриттів розділу, кожне без page error, без console error і без «NaN»: приймальникові «Каса за день», «Ціни дня», «Постачальники», «Залишки», «Ящики» і «Каса точки», а після ВИХОДУ І ВХОДУ ПІД `owner` — «Зведення по сезону», «Собівартість дня», «Переважування», «Середня ціна по мережі», «Аркуш керівника», «Перекази» і ще раз «Постачальники». Перехід між ролями — саме «Вийти» -> «Вхід» -> вхід під іншим записом, а не кнопка: перемикача ролі більше немає, і цей рядок єдиний у файлі проходить signOut(). Шапка звіряється ДВІЧІ, і саме пара робить це твердженням: ДО виходу — «Оксана Г.» під p1, ПІСЛЯ входу — «Керівник» під owner. Другий сам по собі нічого не доводить, бо seed.ts:206 OWNER = «Керівник», тобто імʼя цього запису Й Є назвою ролі, і зашитий рядок ролі пройшов би його без слова (так воно й було до 28.08.2026 — знахідка 8 фінальної рецензії). «Оксана Г.» не збігається з жодною назвою ролі, тому два різні імені на тому самому локаторі не може віддати жодна константа. Різних розділів ДВАНАДЦЯТЬ: «Постачальники» відкриваються двічі навмисно, бо під власником SuppliersPage малює ІНШИЙ компонент (OwnerTable) — інакше цей вигляд не рендерив би жоден тест. Пʼять керівницьких приходять окремими чанками через React.lazy, «Перекази» шостим («Постачальники» — ні, вони в головному, як і нові «Ящики» та «Каса точки»), і в усіх семи під роллю власника чекається саме ВЛАСНИЙ заголовок розділу, а «NaN» перевіряється вже після нього — тому зелене означає відкритий екран, а не Suspense-каркас. Так само по імені заголовка відкриваються «Ящики» і «Каса точки»: там підпис у навігації дослівно дорівнює <h1>. І ЧОТИРИ ЧИСЛА звірені з джерелом — усі з наскрізного прикладу клієнтки (docs/21 §8.4, Шипинки 04.08.2026). Два в самому обході: склад наділу на «Ящиках» (800 = 341 + 195 + 264) і «у шухляді має бути» на «Касі точки» (15 416,10 ₴). Ще два — ОКРЕМИМ тестом на прийомці, у панелі «Стан точки»: та сама тотожність наділу і та сама сума шухляди, бо малює їх там ІНШИЙ код (компактний CrateStandingBar і PointStatePanel), а сусіднє поле тієї самої згортки (berryCash = 1 616,10 ₴) було б цілком правдоподібним числом у гривнях. Тест окремий, бо мусить читати незайманий сід: візит із тесту вище зрушив би і касу, і склад наділу. Обидва ці числа — Шипинки, і точку туди ставить не клік по селектору, а вхід під p1 (scopeAfterSignIn). Тобто зібраний артефакт довозить ці числа до екрана, а не лише рушій до юніт-тесту. | Один браузер, один розмір вікна, тільки демо-дані з seed — і, головне, збереженого payload немає: браузер щоразу починає з порожнім localStorage, тому жодне звуження на шляху rehydrate, яке храповить ratchet:persist, тут не виконується. Зокрема сесія в цьому файлі ЖОДНОГО разу не приїжджає зі сховища — вона щоразу створюється входом, тому ні isSession(), ні перевірка «userId є в реєстрі» тут не виконуються. ВХІД ПЕРЕВІРЕНО ЛИШЕ ВДАЛИЙ: правильна пара логін/пароль подається чотири рази, а три причини відмови (unknown-login, wrong-secret, no-account) не рендеряться тут ніколи — їхні тексти на екрані не бачив жоден тест. Так само не перевірено жодного гейта авторизації: під p1 нічого не пробується робити на чужій точці, а під owner — нічого забороненого приймальникові. Поза обходом лишаються «Журнал», «Точки», «Тара і сорти» і картка постачальника; про них цей рядок не каже нічого. У чотирьох приймальникових розділах чекається БУДЬ-ЯКИЙ заголовок, а не назва екрана: там підпис розділу не дорівнює <h1> («Каса за день» → «Каса за <дата>»), тому підміна екрана на інший непорожній там не видна. Правильність числа звірена у ДВОХ місцях із тринадцяти — склад наділу на «Ящиках» і сума шухляди на «Касі точки»; на решті одинадцяти екранів перевіряється рендер, відсутність помилок і відсутність NaN, і жодна сума там не звірена ні з чим. Панель «Стан точки» на прийомці звіряє ті самі два числа й ЖОДНОГО нового: три касові кахлі з чотирьох («у шухляді на ранок», «видано за ягоду», «залишків створено») не звірені ні з чим, окрім відсутності NaN, і те саме стосується розкладу «за ягоду … · завдатків …» під сіткою. Навіть тест візиту вище стверджує «не нуль і не NaN», а не рівність конкретній сумі. Підпис у квитанції звірено на ОДНОМУ записі з семи: «Оксана Г.» під p1. Що другий касир Шипинок (Марія С., той самий pointId) підпише документ СВОЇМ імʼям — тобто саме те, заради чого фаза 4 і робилася, — тут не перевіряє ніщо. Retries = 0, тому нестабільність видно як червоне, а не як «пройшло з другої спроби». |
| `coverage` | full | Покриття рядків і гілок для src/** (крім src/components/ui/**, src/main.tsx і самих тестів) зміряно і записано у coverage/. Локально це і все, що воно доводить. У CI пороги беруться з env у .github/workflows/verify.yml, і відсутнє або нечислове значення тепер валить перевірку, а не тихо стає нулем. | Виконання — не перевірка. Тест, який викликає функцію і нічого не стверджує, підіймає покриття так само, як справжній. ПЕРЕМІРЯНО 28.08.2026 після виправлень за фінальною рецензією (npm run coverage): src/lib — 94,86 % рядків (1 310 з 1 381) і 86,85 % гілок (984 з 1 133); .tsx — 0,00 % (0 з 1 845 рядків у 42 файлах), тобто рівно нуль і після екрана входу. ОБИДВА ВІДСОТКИ ПО src/lib ВПАЛИ проти 27.08.2026 (95,26 і 88,45), хоч фаза 4 й принесла два повністю покриті модулі (auth.ts, auth-mock.ts): непокрите зосереджене в store.ts — 86,74 % рядків (301 з 347) і 76,49 % гілок (270 з 353), найнижче в усьому src/lib. Три тести, дописані за рецензією, зрушили гілки src/lib на +0,22 п. п. (86,63 -> 86,85) — приріст видно, і він малий: гейт точки коштує кількох гілок, а не десятків. Поріг для .tsx — 0: число показане, але нічого не вимагає, бо компонентних тестів немає жодного. |

<!-- registry-checksum: a4d08a030f532fc925ca33349179c6ba -->
<!-- END:verify-table -->

### Свідомі рішення, які легко «полагодити» назад

- **jsdom і @testing-library НЕ встановлені, і це навмисно.** Другий тестовий стек із
  нулем тестів у ньому був би мертвою залежністю, яку `deadcode` справедливо б і показав.
  Якщо колись знадобляться компонентні тести — ставити стек разом із першим справжнім
  тестом, не раніше.
  **Але не читати це як «компонентний рівень покрито».** Playwright відкриває сторінки у
  справжньому браузері й перевіряє, що вони рендеряться без помилок і без NaN — це не
  покриття рядків і не перевірка жодної суми. Покриття `.tsx` зміряне й дорівнює **0,00 %
  (0 з 1 840 рядків у 42 файлах)** — зміряно `npm run coverage` 28.08.2026; e2e-прогін нічим
  не інструментований. Число росте разом із кодом і лишається нулем: 24.08.2026 було
  1 224 рядки у 24 файлах, 25.08.2026 — 1 617 у 39, 27.08.2026 — 1 819 у 41. Фаза 4 додала
  до цього нуля екран входу: `SignInPage.tsx` не виконує жоден юніт-тест, і те, що кнопка
  «Увійти» кличе саме `signIn()`, тримає лише smoke.
- **`shadcn` живе у `devDependencies`, не в `dependencies`.** Це CLI для скафолдингу, який
  застосунок ніде не імпортує (`components.json` посилається лише на його схему). Спочатку
  він лежав у runtime-залежностях і тягнув за собою 2 з 3 адвайзорі; після переносу вони
  стали dev-only, тобто до бандла не доходять. Не повертати назад.
- **Бюджет розміру бандла: стелю піднято НАПЕРЕД 27.08.2026, і це треба читати як
  послаблення.** Запас у `scripts/verify/baselines/bundle-budget.json` тепер
  **25 839 байтів gzip і 93 324 raw** замість 5 003 і 7 415 — приблизно ціла фаза роботи
  (кожна з фаз 2, 3 і 5-6 давала 13–23 KiB gzip). Причина записана в `note` того файлу:
  рішення власника, щоб перевірка «поки не заважала». Три попередні підняття були іншого
  роду — вони знімалися ПІСЛЯ завершеної фази й лише округлювали зміряне; це зроблено
  наперед, у момент, коли перевірка була **зелена** (342 801 зі стелі 348 160). Отже воно
  не розвʼязало жодної проблеми, а відсунуло момент, коли доведеться. І відсування вже
  видно: фаза 4 забрала 1 963 байти gzip, зміряний запас 28.08.2026 — **23 876 gzip і
  86 218 raw**, тобто число в `headroom*` того файлу — мірка 27.08, а не сьогоднішній
  стан; сьогоднішній друкує сама перевірка на кожному зеленому прогоні.

  Що з цього випливає для читача: наступна регресія має шанс проїхати мовчки. Ловити її
  тепер треба очима у виводі `bundle` — він друкує запас на кожному зеленому прогоні, — а
  не чекати червоного. І **рядок «НАСТУПНЕ ПІДНЯТТЯ ЗАБОРОНЕНО, поки не почищено спільний
  шар» скасовано власником, а сам борг не сплачено**: зміряно 28.08.2026 — `bits`
  39 298 gzip і css 16 932, разом 56 230 на демо-CRM без жодної картинки. Борг не тільки
  не сплачено, він за добу виріс на 612 байтів.

  Нульового запасу тут не було б у жодному разі: у першій версії стеля пінилася байт у
  байт і впала на наступному ж коміті через **18 байтів** gzip. Перевірка, яка вимагає
  правки бюджету через 18 байтів, вчить піднімати бюджет не читаючи — тому стеля
  округлюється вгору кроком 5 KiB (gzip) і 20 KiB (raw), а падати мусить РЕГРЕСІЯ: нова
  бібліотека, випадковий імпорт усього пакета.

  **І головне, чого бюджет не ловить у принципі.** Він міряє СУМУ `dist/assets`, а не те,
  що людина завантажує. Переміряно 28.08.2026: 14 файлів, **336,7 KiB gzip** разом, але
  приймальник на першому відкритті тягне 202,7 KiB (перший чанк 136,0 + `bits` 38,4 +
  css 16,5 + es2015 10,4 + react-dom 1,3) **плюс 208,8 KiB девʼяти файлів шрифтів з
  `fonts.gstatic.com`** — шрифти перевірені в мережевій панелі браузера 27.08.2026, і
  число досі чинне: `index.html` відтоді не змінювався жодним байтом. Тобто стеля стереже
  менше половини того, що реально переїжджає до нього мережею. Тому наступним кроком тут
  має бути не чергове підняття, а зміна самої мірки: **стерегти перший чанк, а не суму**.
  Сума росте й тоді, коли приймальникові стає легше — розділення чанків у фазі 3 зменшило
  його завантаження на 38 %, а суму підняло, і за це довелося піднімати стелю.
- **`npm run preview` містить `--base /yagoda-crm/`.** Без цього прапорця preview віддає
  dist із кореня, а index.html просить `/yagoda-crm/assets/…` — усі ассети 404 і біла
  сторінка. `vite.config.ts` виставляє base лише для `command === 'build'`, а preview
  працює як `serve`. Playwright спеціально запускає саме `npm run preview`, щоб бачити
  той самий артефакт, що й людина.

- **Фаза 4 зробила вхід, і його НЕ МОЖНА називати захистом.** Застосунок відкривається
  екраном входу, кожен підпис під документом — імʼя людини з сесії, а роль більше не поле
  стану, яке ставлять кнопкою. Але пароль перевіряє **той самий браузер**, у якому сидить
  людина, і `docs/22-tz.md §17.2` каже про це прямо: «Пароль, що зберігається й звіряється
  на самій точці, не підтверджує нічого: хто має цей компʼютер, той має і пароль». Те саме
  стосується трьох нових гейтів (ціна дня — лише керівник; зміну відкриває приймальник;
  чужа точка закрита) — вони переносять правило з домовленості в **розрахунок**, а не в
  захист. Екран входу каже це вголос сам, і той абзац на ньому — не прикраса: прибрати його
  означало б почати вдавати.

  Що з цього випливає механічно: секрет живе в `src/lib/auth-mock.ts`, який стоїть НА МІСЦІ
  СЕРВЕРА, експортує рівно одне імʼя і стережеться `seed:port` нарівні з фікстурою сіду.
  Реекспортувати його через `store.ts` не можна — не тому, що чекер це зловить (він
  реекспортів не бачить і сам про це пише), а тому, що це була б та сама діра, яку той
  рядок нібито закриває.

- **Перемикача ролі в демо-панелі більше немає, і повертати його не можна.** Він
  стверджував, що роль — властивість пристрою, яку вибирають кнопкою. `22-tz §18.4` вимагає
  протилежного: «Підміна на зміні — це вихід і вхід під іншим, **а не перемикач**». Ціна
  цього рішення названа: показати другий вигляд тепер коштує два поля й кнопку замість
  однієї секунди (`22-tz` ряд. 663 описував стару кнопку саме як «щоб за одну секунду
  побачити обидва екрани»).

- **Два поля документів змінили ЗНАЧЕННЯ, а не лише джерело.** `CashCount.countedBy` і
  `Shift.closedBy` раніше копіювалися з `Shift.operatorId` — тобто відповідали на питання
  «хто відкрив зміну». Тепер це той, **хто натиснув**. На точці з одним касиром різниці
  немає; на Шипинках, де їх двоє, це різні люди — і саме заради цієї різниці фаза й
  робилася.

Порогів покриття локально немає (підлога 0) — вони живуть лише в
`.github/workflows/verify.yml`, там де їх застосовують. Зниження порога — видима правка
саме в тому файлі.

### CI: лампочка чи гейт

`verify.yml` існує і проходить (перший зелений прогін на раннері GitHub — 13/13, 1 хв 16 с).
Але **CI стає гейтом лише тоді, коли для `main` увімкнено required status check**. Це
налаштування репозиторію, не код, тож із самого коду його не видно і жодна перевірка тут
його не забезпечує.

Станом на 2026-08-23 воно **НЕ ввімкнене**: `branches/main/protection` віддає 404,
`rulesets` — `[]`. Тобто зараз червоний `verify` нікого не спиняє від мерджу в `main`.
Єдине, що вже тримається механічно, — `deploy` має `needs: verify`, тому червоне дерево
не поїде на Pages (сайт просто лишиться на попередній збірці).

Увімкнути (потрібні адмінські права на репозиторій; `gh` на цій машині авторизований як
`swefd`, який є адміном в організації `webspirio`):

```bash
gh api -X PUT repos/webspirio/yagoda-crm/branches/main/protection \
  -F 'required_status_checks[strict]=true' \
  -F 'required_status_checks[contexts][]=verify' \
  -F 'enforce_admins=false' \
  -F 'required_pull_request_reviews=' -F 'restrictions='
```

Перевірити, що подіяло:

```bash
gh api repos/webspirio/yagoda-crm/branches/main/protection \
  --jq '.required_status_checks | {strict, contexts}'
```

Що варто знати перед тим, як вмикати:

- `contexts` мусить містити саме `verify` — так називається job у `verify.yml`. Якщо job
  колись перейменують, захист почне вимагати перевірку, якої більше немає, і мерджі
  застрягнуть назавжди.
- `strict=true` означає, що гілку треба оновити від `main` перед мерджем. Це саме те, що
  закриває дірку «обидві гілки зелені окремо, а разом ламаються».
- `enforce_admins=false` лишає адміну можливість пропхати правку в обхід — свідомо, щоб
  зламаний `verify` не заблокував і той мердж, який його лагодить. Якщо потрібна
  безумовна дисципліна — поставити `true`.
- Вмикати **після** того, як `verify` хоч раз пройшов на реальному прогоні. Вимагати
  перевірку, яка ще ніколи не була зеленою, означає заблокувати всі мерджі, включно з
  виправленням.
- Захист гілки також забороняє прямий `git push` у `main` — працювати доведеться через PR.

## Три правила

### 1. Докази під заявою

**Цю таблицю не перевіряє ніщо.** Гейт наприкінці ходу завжди запускає лише швидкий
рівень; він не дивиться, що саме змінилося, і не вимагає відповідних доказів. Тобто нижче
— зобовʼязання, а не механізм. Механічно тримаються тільки п'ять станів, храповики
baseline, заборона ключів приглушення в `knip.json`, храповик винятків oxlint і пороги
покриття в CI.

Не «зроблено», а «зроблено, перевірено тим-і-тим». Відповідність така:

| що змінилося | які докази обов'язкові |
| --- | --- |
| будь-що в `src/` | `npm run verify` (швидкий рівень) |
| `src/lib/calc.ts`, `store.ts`, `seed.ts` | `npm run verify` + `npm run coverage`, і назвати цифру |
| будь-який `.tsx` | `npm run verify:full` — бо саме smoke, а не unit-тести, торкається компонентів |
| `vite.config.ts`, `index.html`, `public/` | `npm run verify:full` (base path ламається лише в збірці) |
| `package.json`, лок-файл | `npm run verify:full` + `npm run deadcode` |
| будь-що в `scripts/verify/`, `.claude/hooks/` | `npm run verify` — цей код теж під `tsc -b` |
| registry перевірок | `node scripts/verify/checks/memo-drift.mjs --write`, інакше `memo` впаде |
| `.oxlintrc.json` overrides | `npm run lint:exempt` — і оновити `baselines/lint-exempt.json` |
| `.gitignore`, будь-що біля `docs/` та `input/` | `npm run pii` — межа публічного репозиторію |
| `package.json`, лок-файл, нова залежність | `npm run verify:full` — там `audit` і `bundle` |
| будь-що, що впливає на розмір бандла | `npm run verify:full` — там `bundle`; стелю піднято наперед 27.08.2026, тому регресія може проїхати мовчки: дивитися на число запасу у виводі, а не чекати червоного (28.08.2026 запас уже 23 876 байтів gzip із записаних у baseline 25 839) |

Заявляти успіх без прочитаного виводу не можна. Якщо перевірка не запускалася — так і
казати, а не «мабуть, зелено».

### 2. Пропуски проговорюються вголос

«Швидкий рівень зелений; build і smoke пропущено — немає браузера» — і ніколи
«все зелено». Пропуск означає, що про цю частину не перевірено нічого; це інформація, а
не дрібниця, яку можна округлити до успіху.

### 3. Храповики крутяться лише в один бік

Усі baseline у `scripts/verify/baselines/` можуть тільки зменшуватися. Правило
двостороннє: нова знахідка падає, і знахідка, якої вже немає, теж падає — запис треба
прибрати. Причина в кожному записі обов'язкова, і чекер сам відкидає заглушки
(`TODO`, порожній рядок, коротше за 30 символів).

**Позеленіти, розширивши виняток, — це не позеленіти.** Це найзручніша з доступних
раціоналізацій, тому вона написана тут явно. Якщо перевірка червона, а рука тягнеться
дописати запис у baseline, послабити правило oxlint, знизити поріг покриття або додати
ключ приглушення в `knip.json` — це саме та поломка, від якої цей шар і будували.
`deadcode` навіть падає, якщо в `knip.json` з'явився будь-який ключ приглушення.

Виняток дозволений лише коли він: перелічений поштучно, датований, з причиною, яку
перевірено проти джерела, і сам себе скасовує, коли знахідка зникає.
